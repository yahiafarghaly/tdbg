{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "taskName": "build tdbg and debugging examples",
            "type": "shell",
            "command": "mkdir build; cd build; cmake .. ; make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}